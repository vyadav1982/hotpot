{
  "name": "hotpot",
  "description": "Hotpot | Bon without Bone",
  "author": "BytePanda Technologies Pvt. Ltd.",
  "license": "GPL-3.0",
  "aworkspaces": [
    "frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd frontend && yarn dev",
    "build": "cd frontend && yarn build",
    "format": "yarn format-web",
    "postinstall": "yarn install-web && yarn install-desk",
    "install-web": "cd frontend && yarn install",
    "install-desk": "echo 'Skipping nested desk install to avoid infinite loop'",
    "format-web": "cd frontend && yarn format"
  }
}